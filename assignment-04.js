// Use getters and setters to Control Access to an Object

//This assignment has a bug where you don't actually need getters/setters to pass the tests. Instead you can use
// this. , but it seems that really anything you insert will pass so it is unclear what the correct answer actually is.